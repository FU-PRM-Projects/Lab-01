include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  exclude:
    - build/**
    - packages/paper_native/cargokit/**
    - packages/paper_native/lib/src/rust/**
    - windows/**
